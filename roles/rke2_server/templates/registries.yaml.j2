mirrors:
  {{ registry_external }}:
    endpoint:
      - "{{ registry_internal_http_endpoint }}"
configs:
  {{ registry_external }}:
    # 内部は HTTP を叩くため CA 配布は不要。将来 https 化したときの足かせ回避のため false を明示。
    tls:
      insecure_skip_verify: true

